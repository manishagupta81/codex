# Rename this file to `.env` and populate the values below before running the app locally.
# All variables must be prefixed with VITE_ so Vite exposes them to the client.

# Personal access token generated from Calendly (https://calendly.com/integrations/api_webhooks)
VITE_CALENDLY_API_KEY=your-calendly-personal-access-token

# The Calendly organization or user URI (e.g. https://api.calendly.com/users/ABCDEFG123)
VITE_CALENDLY_OWNER_URI=https://api.calendly.com/users/your-user-id

# The public scheduling URL to embed inside the site.
VITE_CALENDLY_EVENT_URL=https://calendly.com/your-workspace/30min

# Domain name where the embed will render (defaults to localhost during development).
VITE_CALENDLY_EMBED_DOMAIN=localhost

# Hex color (with or without #) used to theme the Calendly widget.
VITE_CALENDLY_PRIMARY_COLOR=#7f8ae0

# Optionally point to a different Calendly API base URL (defaults to https://api.calendly.com)
VITE_CALENDLY_API_BASE_URL=https://api.calendly.com
